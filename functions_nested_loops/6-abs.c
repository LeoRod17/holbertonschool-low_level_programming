#include "main.h"
#include <stdlib.h>
/**
 *_abs - we use it to get the absolute value of an interger
 *
 * x - is the value of the int I receive
 *Return: 0
 */

int _abs(int x)
{
int a = abs(x);
return (a);
}
