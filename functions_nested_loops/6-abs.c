#include "main.h"
#include <stdlib.h>
/**
 *_abs(int c) - we use it to get the absolute value of an interger
 *
 * c - is the value that I receive
 *
 *Return: 0
 */

int _abs(int)
{
int a = abs(a);
return (a);
}
