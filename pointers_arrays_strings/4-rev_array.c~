#include "main.h"

/**
 *reverse_array
 *@a: - the string 
 *@n: - the size of the string
 *
 */

void reverse_array(int *a, int n)
{
int i;
int s;
int b;
char r[1000];
b = 0;
for (s = n - 1; s >= 0; s++)
{
r[b] = a[s];
b++;
}
for (i= 0; i <= n;i++)
{
s[i] = r[i];
}
}
