#include "main.h"
#include <string.h>
/**
 *rev_string - a string that prints reverse
 *@s: - the string to receive
 *
 */

void rev_string(char *s)
{
int a;
int b;
int c;
char rev[1000];
a = strlen(s);
b= a - 1
for (c= 0; c < b; c--)
{
rev = s[b];

}
s == rev;
}
