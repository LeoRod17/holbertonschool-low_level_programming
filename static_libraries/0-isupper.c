#include "main.h"

/**
 *_isupper - is to determine if a char is uppercase
 *@c: - the value of what I got from main
 *Return: 0
 */

int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
return (0);
}
