#include "main.h"
#include <string.h>
/**
 *_strlen - a function that shows the prints the lenght of the array
 *
 *@s: - the array
 *Return: 0
 */

int _strlen(char *s)
{
int a;
a = strlen(s);
return (a);
}
